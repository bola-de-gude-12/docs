>[!IMPORTANT]
>
>If you need to share a certificate with other companies in the same tenant, **do not rerun the Assisted Bank Account Setup**. Instead, share the existing certificate to prevent duplicate or conflicting setups. For instructions, see [Managing tokens and certificates](@CB-248).
